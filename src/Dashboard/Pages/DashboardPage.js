import React from "react";
import DPage from "../Components/DPage";

// Assuming that the Dashboard component is in the same directory as DashboardPage

const DashboardPage = () => {
  return (
    <div>
      <DPage />
    </div>
  );
};

export default DashboardPage;
